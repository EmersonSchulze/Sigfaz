@model IEnumerable<Sigfaz.Portal.Areas.Basico.ViewModels.DestinoDespesa.DestinoDespesaIndexViewModel>

@{
    ViewBag.Title = "Destino das Despesas";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Criar Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DestinoDespesaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DestinoDespesaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DestinoDespesaId })
        </td>
    </tr>
}

</table>
