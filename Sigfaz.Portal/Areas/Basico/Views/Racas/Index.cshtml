@model IEnumerable<Sigfaz.Portal.Areas.Basico.ViewModels.Raca.RacaIndexViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abreveatura)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abreveatura)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RacaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RacaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RacaId })
        </td>
    </tr>
}

</table>
