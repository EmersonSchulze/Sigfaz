@model IEnumerable<Sigfaz.Portal.Areas.Basico.ViewModels.ClassificacaoLote.ClassificacaoLoteIndexViewModel>

@{
    ViewBag.Title = "Classificações Lote";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Periodo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Periodo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClassificacaoLoteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClassificacaoLoteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClassificacaoLoteId })
        </td>
    </tr>
}

</table>
